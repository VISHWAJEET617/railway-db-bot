[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt && python3 -m playwright install chromium && python3 -m playwright install-deps chromium"

[deploy]
startCommand = "python3 tgbot/bot.py"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
